import React from "react";

const SectionSupertitle = ({ children }: { children: string }) => {
  return (
    <span className="text-xs text-blue-400 font-semibold">{children}</span>
  );
};

export default SectionSupertitle;
